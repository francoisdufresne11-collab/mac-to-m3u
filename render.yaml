services:
  - type: web
    name: mac-to-m3u
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mac2m3u:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1 --threads 4
    plan: free
